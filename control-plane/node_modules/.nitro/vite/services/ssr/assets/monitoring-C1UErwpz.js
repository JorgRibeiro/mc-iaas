import { a as nodesQuery, c as timeseriesQuery, o as overviewQuery, r as instancesQuery } from "./queries-D6pnQp7P.js";
import { a as PageHeader, n as EmptyState, r as ErrorState, t as CardsSkeleton } from "./StateViews-CO8w9HyP.js";
import { c as Table, d as TableHead, f as TableHeader, i as formatTimestamp, l as TableBody, n as formatMb, o as percentOf, p as TableRow, r as formatPercent, t as formatGb, u as TableCell } from "./format-Bdjq67dT.js";
import { n as MetricBar, t as StatCard } from "./StatCard-VB2AFQE5.js";
import { i as NodeStatusBadge, n as InstanceStateBadge, o as SeverityBadge } from "./StatusBadge-K-Qktd-I.js";
import { Link } from "@tanstack/react-router";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
import { Boxes, Cpu, Database, Gauge, MemoryStick, Server, ShieldAlert } from "lucide-react";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
//#region src/features/monitoring/UsageChart.tsx
function UsageChart({ data, metric, title, subtitle }) {
	const color = metric === "cpu" ? "var(--color-chart-1)" : "var(--color-chart-2)";
	return /* @__PURE__ */ jsxs("div", {
		className: "panel p-4",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "mb-3 space-y-0.5",
			children: [/* @__PURE__ */ jsx("h3", {
				className: "text-sm font-medium",
				children: title
			}), subtitle && /* @__PURE__ */ jsx("p", {
				className: "text-xs text-muted-foreground",
				children: subtitle
			})]
		}), /* @__PURE__ */ jsx("div", {
			className: "h-44 w-full",
			children: /* @__PURE__ */ jsx(ResponsiveContainer, {
				width: "100%",
				height: "100%",
				children: /* @__PURE__ */ jsxs(AreaChart, {
					data,
					margin: {
						top: 4,
						right: 4,
						bottom: 0,
						left: -18
					},
					children: [
						/* @__PURE__ */ jsx("defs", { children: /* @__PURE__ */ jsxs("linearGradient", {
							id: `grad-${metric}`,
							x1: "0",
							y1: "0",
							x2: "0",
							y2: "1",
							children: [/* @__PURE__ */ jsx("stop", {
								offset: "0%",
								stopColor: color,
								stopOpacity: .35
							}), /* @__PURE__ */ jsx("stop", {
								offset: "100%",
								stopColor: color,
								stopOpacity: .02
							})]
						}) }),
						/* @__PURE__ */ jsx(CartesianGrid, {
							stroke: "var(--color-border)",
							vertical: false
						}),
						/* @__PURE__ */ jsx(XAxis, {
							dataKey: "t",
							tick: {
								fontSize: 11,
								fill: "var(--color-muted-foreground)"
							},
							stroke: "var(--color-border)"
						}),
						/* @__PURE__ */ jsx(YAxis, {
							domain: [0, 100],
							tick: {
								fontSize: 11,
								fill: "var(--color-muted-foreground)"
							},
							stroke: "var(--color-border)",
							unit: "%"
						}),
						/* @__PURE__ */ jsx(Tooltip, {
							contentStyle: {
								background: "var(--color-popover)",
								border: "1px solid var(--color-border)",
								borderRadius: 8,
								fontSize: 12
							},
							labelStyle: { color: "var(--color-muted-foreground)" },
							formatter: (value) => [`${value}%`, metric === "cpu" ? "CPU" : "Memory"]
						}),
						/* @__PURE__ */ jsx(Area, {
							type: "monotone",
							dataKey: metric,
							stroke: color,
							strokeWidth: 2,
							fill: `url(#grad-${metric})`
						})
					]
				})
			})
		})]
	});
}
//#endregion
//#region src/routes/monitoring.tsx?tsr-split=component
function MonitoringPage() {
	const overview = useQuery(overviewQuery);
	const nodes = useQuery(nodesQuery);
	const instances = useQuery(instancesQuery);
	const timeseries = useQuery(timeseriesQuery);
	const error = overview.error ?? nodes.error ?? instances.error ?? timeseries.error;
	const summary = overview.data;
	const nodeList = nodes.data;
	const instanceList = instances.data;
	const timeseriesData = timeseries.data;
	if (error) return /* @__PURE__ */ jsx(ErrorState, {
		message: error.message,
		onRetry: () => void Promise.all([
			overview.refetch(),
			nodes.refetch(),
			instances.refetch(),
			timeseries.refetch()
		])
	});
	const invariants = (nodeList ?? []).flatMap((node) => node.invariants.map((invariant) => ({
		...invariant,
		nodeId: node.id,
		nodeName: node.name
	}))).sort((a, b) => b.timestamp.localeCompare(a.timestamp));
	const distribution = [
		"running",
		"stopped",
		"starting",
		"unavailable",
		"deleting"
	].map((state) => ({
		state,
		count: instanceList?.filter((instance) => instance.state === state).length ?? 0
	}));
	return /* @__PURE__ */ jsxs(Fragment, { children: [
		/* @__PURE__ */ jsx(PageHeader, {
			title: "Monitoring",
			description: "Aggregated health, utilization and invariant signals across the mock infrastructure."
		}),
		!summary ? /* @__PURE__ */ jsx(CardsSkeleton, { count: 4 }) : /* @__PURE__ */ jsxs("div", {
			className: "grid gap-3 sm:grid-cols-2 xl:grid-cols-4",
			children: [
				/* @__PURE__ */ jsx(StatCard, {
					label: "Infrastructure health",
					value: summary.status === "operational" ? "Operational" : summary.status === "degraded" ? "Degraded" : "Down",
					icon: Gauge,
					caption: `${summary.nodesOnline}/${summary.nodesTotal} nodes online`
				}),
				/* @__PURE__ */ jsx(StatCard, {
					label: "Aggregate capacity",
					value: `${summary.slotsUsed}/${summary.slotsTotal}`,
					icon: Server,
					bar: {
						used: summary.slotsUsed,
						total: summary.slotsTotal
					},
					caption: "Runtime slots occupied"
				}),
				/* @__PURE__ */ jsx(StatCard, {
					label: "Memory pressure",
					value: formatPercent(percentOf(summary.memoryUsedMb, summary.memoryTotalMb)),
					icon: MemoryStick,
					bar: {
						used: summary.memoryUsedMb,
						total: summary.memoryTotalMb
					},
					caption: `${formatMb(summary.memoryUsedMb)} used`
				}),
				/* @__PURE__ */ jsx(StatCard, {
					label: "Storage pressure",
					value: formatPercent(percentOf(summary.storageUsedGb, summary.storageTotalGb)),
					icon: Database,
					bar: {
						used: summary.storageUsedGb,
						total: summary.storageTotalGb
					},
					caption: `${formatGb(summary.storageUsedGb)} used`
				})
			]
		}),
		/* @__PURE__ */ jsx("div", {
			className: "grid gap-4 xl:grid-cols-2",
			children: !timeseriesData ? /* @__PURE__ */ jsx(CardsSkeleton, { count: 2 }) : /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(UsageChart, {
				data: timeseriesData,
				metric: "cpu",
				title: "Aggregate CPU",
				subtitle: "Recent utilization across reachable nodes"
			}), /* @__PURE__ */ jsx(UsageChart, {
				data: timeseriesData,
				metric: "memory",
				title: "Aggregate memory",
				subtitle: "Recent working-set utilization"
			})] })
		}),
		/* @__PURE__ */ jsxs("div", {
			className: "grid gap-4 xl:grid-cols-2",
			children: [/* @__PURE__ */ jsxs("section", {
				className: "panel p-4",
				children: [/* @__PURE__ */ jsxs("div", {
					className: "mb-4",
					children: [/* @__PURE__ */ jsx("h2", {
						className: "text-sm font-medium",
						children: "Node health"
					}), /* @__PURE__ */ jsx("p", {
						className: "mt-0.5 text-xs text-muted-foreground",
						children: "Current reachability and key resource pressure."
					})]
				}), !nodeList ? /* @__PURE__ */ jsx(CardsSkeleton, { count: 2 }) : /* @__PURE__ */ jsx("div", {
					className: "space-y-4",
					children: nodeList.map((node) => /* @__PURE__ */ jsxs("div", {
						className: "rounded-md border border-border p-3",
						children: [/* @__PURE__ */ jsxs("div", {
							className: "mb-3 flex items-center justify-between gap-3",
							children: [/* @__PURE__ */ jsx(Link, {
								to: "/nodes/$nodeId",
								params: { nodeId: node.id },
								className: "text-sm font-medium hover:text-primary",
								children: node.name
							}), /* @__PURE__ */ jsx(NodeStatusBadge, { status: node.status })]
						}), /* @__PURE__ */ jsxs("div", {
							className: "grid gap-3 sm:grid-cols-3",
							children: [
								/* @__PURE__ */ jsx(MetricBar, {
									label: "CPU",
									used: node.metrics.cpu.usagePercent,
									total: 100,
									hint: formatPercent(node.metrics.cpu.usagePercent)
								}),
								/* @__PURE__ */ jsx(MetricBar, {
									label: "Memory",
									used: node.metrics.memory.usedMb,
									total: node.metrics.memory.totalMb,
									hint: formatPercent(percentOf(node.metrics.memory.usedMb, node.metrics.memory.totalMb))
								}),
								/* @__PURE__ */ jsx(MetricBar, {
									label: "Storage",
									used: node.metrics.mcIaasDisk.usedGb,
									total: node.metrics.mcIaasDisk.totalGb,
									hint: formatPercent(percentOf(node.metrics.mcIaasDisk.usedGb, node.metrics.mcIaasDisk.totalGb))
								})
							]
						})]
					}, node.id))
				})]
			}), /* @__PURE__ */ jsxs("section", {
				className: "panel p-4",
				children: [
					/* @__PURE__ */ jsxs("div", {
						className: "mb-4",
						children: [/* @__PURE__ */ jsx("h2", {
							className: "text-sm font-medium",
							children: "Instance state distribution"
						}), /* @__PURE__ */ jsx("p", {
							className: "mt-0.5 text-xs text-muted-foreground",
							children: "Workload inventory grouped by lifecycle state."
						})]
					}),
					/* @__PURE__ */ jsx("div", {
						className: "space-y-3",
						children: distribution.map(({ state, count }) => /* @__PURE__ */ jsxs("div", {
							className: "flex items-center gap-4 rounded-md border border-border px-3 py-2.5",
							children: [
								/* @__PURE__ */ jsx(InstanceStateBadge, { state }),
								/* @__PURE__ */ jsx("div", {
									className: "h-1.5 flex-1 overflow-hidden rounded-full bg-muted",
									children: /* @__PURE__ */ jsx("div", {
										className: "h-full rounded-full bg-primary",
										style: { width: `${instanceList?.length ? count / instanceList.length * 100 : 0}%` }
									})
								}),
								/* @__PURE__ */ jsx("span", {
									className: "tabular w-6 text-right text-sm font-semibold",
									children: count
								})
							]
						}, state))
					}),
					/* @__PURE__ */ jsxs("div", {
						className: "mt-4 grid grid-cols-3 gap-3 border-t border-border pt-4",
						children: [
							/* @__PURE__ */ jsx(SmallStat, {
								icon: Boxes,
								label: "Total",
								value: instanceList?.length ?? 0
							}),
							/* @__PURE__ */ jsx(SmallStat, {
								icon: Cpu,
								label: "Running",
								value: distribution.find((item) => item.state === "running")?.count ?? 0
							}),
							/* @__PURE__ */ jsx(SmallStat, {
								icon: ShieldAlert,
								label: "Unavailable",
								value: distribution.find((item) => item.state === "unavailable")?.count ?? 0
							})
						]
					})
				]
			})]
		}),
		/* @__PURE__ */ jsxs("section", {
			className: "space-y-3",
			children: [/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("h2", {
				className: "text-sm font-semibold",
				children: "Recent invariants"
			}), /* @__PURE__ */ jsx("p", {
				className: "mt-0.5 text-xs text-muted-foreground",
				children: "Consistency violations reported by compute agents."
			})] }), invariants.length === 0 ? /* @__PURE__ */ jsx(EmptyState, {
				title: "No invariant violations",
				description: "All reachable nodes report internally consistent state."
			}) : /* @__PURE__ */ jsx("div", {
				className: "panel overflow-x-auto",
				children: /* @__PURE__ */ jsxs(Table, { children: [/* @__PURE__ */ jsx(TableHeader, { children: /* @__PURE__ */ jsxs(TableRow, {
					className: "hover:bg-transparent",
					children: [
						/* @__PURE__ */ jsx(TableHead, { children: "Severity" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Node" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Code" }),
						/* @__PURE__ */ jsx(TableHead, { children: "Detail" }),
						/* @__PURE__ */ jsx(TableHead, {
							className: "text-right",
							children: "Timestamp"
						})
					]
				}) }), /* @__PURE__ */ jsx(TableBody, { children: invariants.map((invariant) => /* @__PURE__ */ jsxs(TableRow, { children: [
					/* @__PURE__ */ jsx(TableCell, { children: /* @__PURE__ */ jsx(SeverityBadge, { severity: invariant.severity }) }),
					/* @__PURE__ */ jsx(TableCell, { children: /* @__PURE__ */ jsx(Link, {
						to: "/nodes/$nodeId",
						params: { nodeId: invariant.nodeId },
						className: "text-sm font-medium hover:text-primary",
						children: invariant.nodeName
					}) }),
					/* @__PURE__ */ jsx(TableCell, {
						className: "tabular text-xs",
						children: invariant.code
					}),
					/* @__PURE__ */ jsx(TableCell, {
						className: "max-w-2xl text-xs text-muted-foreground",
						children: invariant.detail
					}),
					/* @__PURE__ */ jsx(TableCell, {
						className: "tabular text-right text-xs whitespace-nowrap",
						children: formatTimestamp(invariant.timestamp)
					})
				] }, invariant.id)) })] })
			})]
		})
	] });
}
function SmallStat({ icon: Icon, label, value }) {
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsx(Icon, { className: "mb-2 h-4 w-4 text-muted-foreground" }),
		/* @__PURE__ */ jsx("p", {
			className: "metric-label",
			children: label
		}),
		/* @__PURE__ */ jsx("p", {
			className: "tabular mt-1 text-lg font-semibold",
			children: value
		})
	] });
}
//#endregion
export { MonitoringPage as component };
