import { m as Button, t as eventsQuery } from "./queries-D6pnQp7P.js";
import { a as PageHeader, i as TableSkeleton, n as EmptyState, r as ErrorState } from "./StateViews-CO8w9HyP.js";
import { t as EventsTable } from "./EventsTable-BIcR367i.js";
import { useState } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
//#region src/routes/activity.tsx?tsr-split=component
function ActivityPage() {
	const events = useQuery(eventsQuery);
	const [filter, setFilter] = useState("all");
	const filtered = events.data?.filter((event) => filter === "all" || event.level === filter) ?? [];
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(PageHeader, {
		title: "Activity",
		description: "Chronological lifecycle, runtime, recovery and security events emitted by the mock Control Plane.",
		actions: /* @__PURE__ */ jsx("div", {
			className: "flex rounded-md border border-border bg-muted/40 p-1",
			children: [
				"all",
				"info",
				"warning",
				"error"
			].map((level) => /* @__PURE__ */ jsx(Button, {
				variant: filter === level ? "secondary" : "ghost",
				size: "sm",
				className: "h-7 capitalize",
				onClick: () => setFilter(level),
				children: level
			}, level))
		})
	}), events.isPending ? /* @__PURE__ */ jsx(TableSkeleton, { rows: 8 }) : events.isError ? /* @__PURE__ */ jsx(ErrorState, {
		message: events.error.message,
		onRetry: () => void events.refetch()
	}) : filtered.length ? /* @__PURE__ */ jsx(EventsTable, { events: filtered }) : /* @__PURE__ */ jsx(EmptyState, {
		title: `No ${filter} events`,
		description: "Try a different severity filter."
	})] });
}
//#endregion
export { ActivityPage as component };
