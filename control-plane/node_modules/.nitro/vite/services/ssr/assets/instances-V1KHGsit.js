import { a as nodesQuery, m as Button, r as instancesQuery } from "./queries-D6pnQp7P.js";
import { t as CreateInstanceDialog } from "./CreateInstanceDialog-CaMh_tQ2.js";
import { a as PageHeader, i as TableSkeleton, n as EmptyState, r as ErrorState } from "./StateViews-CO8w9HyP.js";
import { t as InstancesTable } from "./InstancesTable-MRTh1GPM.js";
import { useState } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
import { Plus } from "lucide-react";
//#region src/routes/instances/index.tsx?tsr-split=component
function InstancesPage() {
	const instances = useQuery(instancesQuery);
	const nodes = useQuery(nodesQuery);
	const [createOpen, setCreateOpen] = useState(false);
	const error = instances.error ?? nodes.error;
	const instanceList = instances.data;
	const nodeList = nodes.data;
	return /* @__PURE__ */ jsxs(Fragment, { children: [
		/* @__PURE__ */ jsx(PageHeader, {
			title: "Instances",
			description: "Minecraft workloads managed as virtual-machine instances across the compute fleet.",
			actions: /* @__PURE__ */ jsxs(Button, {
				size: "sm",
				onClick: () => setCreateOpen(true),
				children: [/* @__PURE__ */ jsx(Plus, { className: "h-4 w-4" }), " Create instance"]
			})
		}),
		error ? /* @__PURE__ */ jsx(ErrorState, {
			message: error.message,
			onRetry: () => void Promise.all([instances.refetch(), nodes.refetch()])
		}) : !instanceList || !nodeList ? /* @__PURE__ */ jsx(TableSkeleton, { rows: 5 }) : instanceList.length === 0 ? /* @__PURE__ */ jsx(EmptyState, {
			title: "No instances provisioned",
			description: "Create a workload to populate the control plane inventory.",
			action: /* @__PURE__ */ jsx(Button, {
				size: "sm",
				className: "mt-2",
				onClick: () => setCreateOpen(true),
				children: "Create instance"
			})
		}) : /* @__PURE__ */ jsx(InstancesTable, {
			instances: instanceList,
			nodes: nodeList
		}),
		/* @__PURE__ */ jsx(CreateInstanceDialog, {
			open: createOpen,
			onOpenChange: setCreateOpen
		})
	] });
}
//#endregion
export { InstancesPage as component };
